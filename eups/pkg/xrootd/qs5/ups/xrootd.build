# lsst build script for protobuf
# # 
curl -L http://www.slac.stanford.edu/exp/lsst/qserv/download/current/xrootd-@VERSION@.tar.gz > @PRODUCT@-@VERSION@.tar.gz &&
gunzip < @PRODUCT@-@VERSION@.tar.gz | tar -xf - &&
product_dir=$(eups path 0)/$(eups flavor)/@PRODUCT@/@VERSION@ &&
echo $PWD &&
cmake ./xrootd -DCMAKE_INSTALL_PREFIX=$product_dir -DENABLE_PERL=FALSE &&
make &&
make install 
